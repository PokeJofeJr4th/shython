value = int(input(chr(62) + chr(32)))
print(value)
while value != 1:
    if value % 2 == 1:
        value = 3 * value + 1
        print(value)
    if value % 2 == 0:
        value = int(value / 2)
    print(value)